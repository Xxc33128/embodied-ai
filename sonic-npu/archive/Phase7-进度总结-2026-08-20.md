# Phase 7 进度总结与问题清单（2026-08-20）

> 目标：接口门禁——证明 Isaac 与 MuJoCo 在 29-DoF SONIC checkpoint 接口下"说同一种语言"，物理调参才有意义。
> 里程碑：全部完成后 `gate-interface` 出报告。此阶段**禁止动物理参数**。

---

## 一、已完成 ✅

### 1. 网格资产补齐（G1 meshes）
- 仓库 sanitized 后**没有任何 STL 网格**，XML `<compiler meshdir="meshes"/>` 引用全部缺失 → `export-mujoco` 直接报错
- 解决：从公开 fork（zhangqin200182/GR00T-WholeBodyControl）的 **LFS media 端点**下载 68 个 STL（58MB），存到 `gear_sonic_deploy/g1/meshes/`（**不入 git**，符合大文件政策）
- 格式混杂问题（ASCII/binary 混排、GBK 乱码头、3dsMax 导出变体）→ 写了转换脚本统一为**规范 binary STL**
- 并行下载导致 2 个文件损坏（`mv: Device busy` 残留），单独重下修复
- 最终：**XML 引用的 36 个 mesh 全部就位且可解析**
- 3 个未转换文件（NVBP.STL / torso_link_23dof_rev_1_0 / yam_gripper_base）**未被 g1_29dof.xml 引用**，不影响

### 2. MuJoCo 侧模型审计 ✅
- 命令：`export-mujoco --model-xml gear_sonic_deploy/g1/g1_29dof.xml --decimation 10`
- 产出：`E:/Temp/audit_mujoco_g1_29dof.json`（70KB，8 sections：actuators/bodies/collisions/joints/metadata/schema_version/solver/unmapped）
- **纯 mujoco，无 Kit 启动，秒级完成**

### 3. 双引擎审计对比 diff ✅
- 命令：`diff --left audit_isaac_g1_model12.json --right audit_mujoco_g1_29dof.json`
- 产出（`artifacts/cross_engine/run1/audit/`）：
  - `model_diff.csv`（2179 行）
  - `unmapped_parameters.json`（233KB）
  - `audit_summary.md`

### 4. 审计对比画像

| 维度 | Isaac | MuJoCo | 备注 |
|---|---|---|---|
| Bodies | 30 | 30 | ✅ 完全一致 |
| Joints | 29 | 29 | ✅ 完全一致 |
| Actuators | 29 | 29 | ✅ 完全一致 |
| Colliders | 0（预构建圆柱模型） | 40 | 差异**预期内**，Phase 10 碰撞族处理 |
| Physics dt | 0.005 | 0.002 | 合规：引擎保留原生语义 |
| Decimation | 4 | 10 | 控制周期同为 20ms |

- 差异统计：Equal 615 / Different 830 / Missing 729（Isaac 侧无 collider）+ semantic/behavioral 需解释或配对行为测试
- **没有设置物理 pass/fail 阈值**（符合手册：第一组结果只记录误差）

---

## 二、当前问题与阻塞点 ⛔

### P0. git 工作树脏 → 接口 trace 无法满足 provenance 要求
- **现状**：`feature/isaac-mujoco-alignment` 分支有 4 处修改 + 6 个新文件未提交（今天 P0/P1 采集链的全部修复）
- **为什么阻塞**：capture-interface 的 metadata 要求 `source_git_commit`（40 位小写 SHA）+ `checkpoint_sha256` 等双侧一致；fingerprint 要求 `source_git.dirty == false`
- **需要用户决策**：是否现在提交？（计划：拆两个 commit——采集修复链 + 采集工具脚本）

### P1. capture adapter 是仓库故意留的缺口（Phase 7 核心工作量）
- README 原文："The existing Isaac and MuJoCo **benchmark** adapters are not interface-capture adapters…This repository **has not verified** that external factory against Windows Isaac yet."
- 需要新写：
  1. **Isaac capture factory**（复用今天跑通的采集 env，注入确定性状态）
  2. **MuJoCo capture factory**（同协议在 MuJoCo runtime 取观测）
  3. **same-state injection**（两侧注入完全相同的 100 个状态；两条自然 rollout 不算数）
  4. 5 个观测块 × 历史10 → `30/30/290/290/290` = **930 维**（gravity_dir/base_ang_vel/joint_pos/joint_vel/last_action）
  5. metadata 必须声明：噪声/随机化/auto-reset/随机策略全禁用 + 全部 provenance SHA

### P2. 挂起/次要项
- 2 个 GitHub PAT 已泄露（`github_pat_[已脱敏]`、`[已脱敏]`，断片已从本副本删除）——**请尽快在 GitHub 撤回**
- git-lfs 未安装（本轮用 media 端点直连绕过了，尚不需要）
- 内存纪律照旧（16GB 机器，Isaac 采集单进程 ~5.3GB，只能串行）

---

## 三、下一步计划（等用户确认）

1. **【等决策】提交当前改动** → 工作树干净，满足 provenance
2. 写 `isaac_capture.py` / `mujoco_capture.py` 两个 adapter factory（复用采集 env 构造）
3. 构造 100 个确定性状态集（state_set SHA256 两侧一致）
4. 跑两侧 `capture-interface --frames 100`
5. `gate-interface --reference isaac --candidate mujoco --report gate_report.json`
6. 之后进入 Phase 8（18-case 成对 benchmark）

---

## 四、资产位置速查

| 资产 | 路径 | 入 git? |
|---|---|---|
| MuJoCo G1 XML | `gear_sonic_deploy/g1/g1_29dof.xml` | 是 |
| G1 网格 68 个 | `gear_sonic_deploy/g1/meshes/` | **否**（58MB） |
| Isaac 审计 | `artifacts/cross_engine/windows/audit_isaac_g1_model12.json` | 否（artifacts 被 ignore） |
| MuJoCo 审计 | `E:/Temp/audit_mujoco_g1_29dof.json` | 否 |
| diff 产出 | `artifacts/cross_engine/run1/audit/` | 否 |
| P0 采集 | `artifacts/cross_engine/windows/baseline/`（24 npz） | 否 |
| P1 采集 | `artifacts/cross_engine/windows/p1/`（27 npz） | 否 |