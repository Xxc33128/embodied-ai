# Phase 7 执行完成报告（2026-08-20 深夜，按用户《Phase7-Windows执行清单-2026-08-20.md》）

> **接口门禁 PASSED**——全部 8 步完成。产出物在 `E:\GR00T-alignment\artifacts\cross_engine\run1\`。

---

## 完成回复（清单第 10 节模板）

```text
Phase 7 Windows 执行结果

Git commit:                9225160（HEAD；本会话共 5 个新 commit）
Git working tree clean:    yes
GitHub repository visibility: 未 push（push 前将再次确认 PRIVATE）
Leaked PAT revoked:        待用户操作（扫描确认仓库内无 token 残留）

Environment check:         pass（沿用既有检查；verify_windows_env 未重跑）
Isaac Sim / Isaac Lab:     5.1.0 / 0.48.0
PhysX / MuJoCo:            Kit 内置 / mujoco(venv)
Python / GPU / driver:     3.11.9 / RTX 5060 Laptop / 580.88 / torch 2.7.0+cu128

Fingerprint generated:     yes
  source_git.dirty:        false
  checkpoint SHA256:       e6bdab3f64a3...（完整见 fingerprint.json）
  motion SHA256:           230352c65ce6...（big_heavy_one_hand...A509.pkl）
  state-set SHA256:        61f5e762d989...

Isaac  bodies/joints/actuators/colliders: 30 / 29 / 29 / 54
MuJoCo bodies/joints/actuators/colliders: 30 / 29 / 29 / 40
Isaac collider exporter fixed:            yes（re-author，isaacsim 5.1 #461）

Isaac capture frames:      100（isaac_golden.npz，readback max|dq|=2.9e-8）
MuJoCo capture frames:     100（mujoco_candidate.npz，readback 精确 0）
Interface gate:            PASS ✅（14/14 exact + 12/12 blocks）
Gate report path:          artifacts/cross_engine/run1/interface/gate_report.json

Known failures:
  - 4 个既有 pytest 失败（test_assets/benchmark_compare，与本次改动无交集）
  - ruff 2 个既有 lint（tests/cross_engine:1:1）
  - 手部 18 个 mesh 碰撞未补（Phase 8 前视需要决策）
  - P0/P1 历史采集为无碰撞状态（Phase 8 必须重采）
Files returned:            commit sha + fingerprint.json(脱敏) + audit_summary.md + gate_report.json + 本报告
Files intentionally kept local: isaac_golden.npz / mujoco_candidate.npz / state_set.json / last.pt / STL /
```

---

## 本次会话产出清单（5 个 commit，未 push）

| commit | 内容 |
|---|---|
| `f42d058` | fix: repair Windows capture runtime（close 挂死/statvfs/resource） |
| `afbed16` | feat: deterministic capture tooling（P0/P1 采集 + 4 wrapper） |
| `dd0b6b2` | fix: re-author URDF primitive colliders（isaacsim 5.1 #461） |
| `a0eb25d` | feat: same-state interface capture adapters（isaac+mujoco） |
| `9225160` | chore: ignore downloaded G1 STL meshes |

## 新增代码结构

```
gear_sonic/cross_engine/
  capture_adapters/
    __init__.py
    common.py          # state set 加载/校验、同公式控制管线、帧组装、metadata
    isaac.py           # Kit 侧 capture adapter（headless+d3d12，同状态注入）
    mujoco.py          # MuJoCo 侧 capture adapter（mj_forward 注入）
    make_state_set.py  # 100-state 确定性生成器（10 类覆盖，seed 无关）
  exporters/
    isaac_colliders.py # 5.1 importer #461 碰撞 re-author（新）
    isaac.py           # 集成 populate + EULA env + _flatten_numbers
```

## 关键验收数字

- gate_report：`passed: true`，ref=isaac，cand=mujoco，frames=100
- observation/target/action 全帧 diff = 0（同源状态集）
- torque unclipped/applied：abs ≤ 2.96e-6（阈值 1e-4），rel ≤ 1e-5 ✓
- fingerprint：dirty=false，无路径/用户名泄漏
- Isaac colliders 0 → 54（26 cylinder + 1 sphere re-author；手部 mesh 18 跳过）

## 待办

1. **3 大决策**：① push 5 个 commit（先确认 PRIVATE）② revoke 2 个泄露 PAT ③ 手部 mesh 是否补
2. Phase 8：18-case 成对 benchmark（benchmark run + run_windows_suite.ps1）—— 现在 Isaac collider 已修复、环境可复用