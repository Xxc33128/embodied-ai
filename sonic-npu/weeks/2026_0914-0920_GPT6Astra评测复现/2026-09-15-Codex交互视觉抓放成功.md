# Codex 交互视觉抓放：一次成功探索

> v1.0，2026-09-15。用户要求当前助手亲自尝试场景。本次是人工编排工具通信、由当前 Codex 根据画面逐次决策的探索运行，不是正式 Astra API 评测，也不进入20 trials统计。

**结果：成功，12次 move_to，276个物理步。** 第12次张开夹爪后，环境返回 success；最终腕视图显示红块已在碗内。[E2：本轮运行]

## 条件与执行边界

- 当前 robosuite-bowl 后端、默认执行噪声、三路224²相机、seed=20260915；上限20次动作决策、350物理步。[E2]
- 决策读取相机与机器人本体状态。使用环境文档中已有的桌高和碗位置说明；未向决策端提供物块真值坐标。桥接程序把环境info保存为评分记录，不回传作动作依据。[E1：本次bridge.py；E2]
- 每次使用现有 `build_toolset(..., max_speed_frac=0.25)` 的 `move_to` 插值动作，逐步交给原适配器；未修改生产环境或控制器，未添加沉降步。到终止即停止。[E1/E2]
- 这是直接工具桥接，不是 `eval()/LLMAgentPolicy` 的完整API调用路径；未复现该路径的全部approver和wire记录。当前助手已有代码与诊断背景，决策次数也不等同于独立API请求次数，因此不能把此次成功率与GLM两次或原报告直接对比。[E1/E3]

## 观察与动作

| 决策 | 行为 | 累计物理步 |
|---|---|---|
| 1 | 张爪，按图像横向对齐并悬停 | 39 |
| 2–7 | 持续下探、小幅对齐；以实际位置确认尚未到位 | 55 |
| 8 | 目标z=0.823，闭合；实测开度约0.493 | 135 |
| 9 | 显式保持gripper=0并提升；侧视确认物块离桌 | 175 |
| 10 | 搬向碗心；实际x仅到0.109，继续观察 | 215 |
| 11 | 保持闭合，继续对齐并下降；实际x=0.148、y=0.005、z=0.926 | 255 |
| 12 | 在碗口上方张开；物块落入碗内，success终止 | 276 |

[E2：逐轮观测、动作与截图]

## 对后续工作的意义

当前场景和接口至少存在一条可由视觉反馈完成的路径，无需为这次任务重新研发控制器。[E2/E3]

但执行时间依赖夹爪参数仍很明显：纯下降调用只有2–4步；夹住方块后实测开度约0.49，显式写gripper=0会让工具按开度差插值约40步，即使二值夹爪已经处于闭合状态。这次提升/搬运使用了该正常工具调用方式，未另加等待。不能据此认定此前GLM失败完全来自视觉或规划能力；执行时序语义仍需统一。[E1/E2/E3]

## 本地证据

工作目录：`gpt6astra-repro/logs-codex-interactive-20260915-101101/`。包含config.json（源文件SHA256与条件）、result.json、events.jsonl、bridge.py与每轮三路PNG；12-wrist.png为最终结果。桥接器只负责命令转发和留痕，不生成抓放动作。原环境文件与既有日志未改动。
