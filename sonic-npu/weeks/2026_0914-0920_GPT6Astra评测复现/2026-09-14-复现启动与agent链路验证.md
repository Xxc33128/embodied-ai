# 2026-09-14 复现启动：环境就绪 + agent loop 全链路本地验证

## 结论（TL;DR）

1. **agent loop 全链路已在 Mac 零成本跑通（E1）**：内置 mock LLM 服务器（OpenAI chat-completions 兼容）驱动 `LLMAgentPolicy` 在 CubePick mock 世界完成 3 trials 闭环评测——chat wire、`move_by` 工具调用、插值限速、预算控制、transcript/action/wire 三层日志全部验证。2/3 成功（第 3 个撞 20 次调用预算线，复现了预算耗尽路径）。
2. **本地 harness 版本 0.58.0 与 RoboCurve 评测所用版本完全一致（E2）**：`inspect-robots==0.58.0`、`inspect-robots-agent==0.26.0`（PyPI）。
3. **RoboCurve agent policy 参数已在源码中定位并复现对齐（E1）**：`max_llm_calls=20` + `max_speed_frac=0.25` + `effort=medium`（`plugins/inspect-robots-agent/src/inspect_robots_agent/policy.py` 的 `AgentPolicyConfig`）。
4. **关键协议事实（E1，源码）**：
   - 工具面由 embodiment 动作空间自动生成：`move_to`（绝对 eef pose）/ `move_by`（增量）/ `move_joints`（关节绝对），另有 `done`/`give_up`/`take_pic`；每次调用必须带 `note`（人类可读的观察-决策说明，进 transcript）。
   - 观测编码：状态以 `state[key]: [数值]` 文本 + 相机帧 PNG data URL 进入用户消息（`policy.py::_observation_content`）。
   - 自定义 OpenAI 兼容端点一等公民支持：`base_url` + `api_key_env`（`_llm.py::resolve_provider`）→ GLM-4.6V 接入无需任何魔改。
   - isaacsim 插件**不打包 Isaac 依赖**（惰性导入），动作空间固定 `joint_pos`（Franka 7 关节+夹爪=8 维）→ Isaac 路线的工具是 `move_joints` 而非 RoboCurve 真机的 eef `move_to`，**这是必须标注的协议差异**；默认任务 `Isaac-Lift-Cube-Franka-v0`。
5. Kaggle T4 版本组合钉死：`isaacsim==5.0.0.0`（PyPI）+ `IsaacLab v2.3.2`——即 8 月云 3060 实证跑通的同一组合（E2，出处 `GR00T-WholeBodyControl/docs/GPU环境搭建指导-IsaacSim.md`）。

## 今日动作

| # | 动作 | 结果 |
|---|---|---|
| 1 | 建周目录 + readme 骨架 | 完成 |
| 2 | Mac 环境：uv venv (py3.11) + `inspect-robots[rerun]` + `inspect-robots-agent` + ffmpeg (brew) | 完成 |
| 3 | clone `robocurve/inspect-robots` 源码（工作区根 `inspect-robots/`）做接口锚点 | 完成 |
| 4 | 零成本冒烟：`ScriptedPolicy` + `CubePickEmbodiment`（examples/quickstart.py） | 5 场景×2 epochs=10 trials，success_at_end=1.0 |
| 5 | **mock LLM 服务器版全链路**（`run_mock_agent.py`）：服务器解析观测文本中的 eef/cube 状态→输出 `move_by` 工具调用 | 3 trials / 56 次 LLM 调用 / success 2/3；日志含 transcripts+actions+wire+PNG blobs |
| 6 | Kaggle 侦察+安装 notebook 成稿（`gpt6astra-repro/kaggle_recon_and_setup.py`，7 cells：侦察→Vulkan→安装→boot proof→LiftCube 冒烟→mock 全链路） | 待用户在 Kaggle 执行 |
| 7 | 正式评测脚本 `eval_bowl.py`（模型/本体/trials 可配，参数默认对齐 RoboCurve） | 待 API key |

## 与 RoboCurve 评测的对照要点（协议层，E1）

| 项 | RoboCurve 真机 | 我们的复现 |
|---|---|---|
| harness | Inspect Robots 0.58.0 | inspect-robots 0.58.0（PyPI，一致） |
| 本体 | I2RT YAM 双臂（eef `move_to`，机器人侧 IK） | mock: eef_delta_pos；Isaac: Franka `move_joints`（差异已标注） |
| 预算/限速 | 20 次调用 / 25% 速度上限 / medium effort | 同参数复现 |
| 观测 | 三路相机+本体状态 | mock 自带相机（已验证 wire 捕获）；Isaac: base_rgb 224² |
| 留痕 | 转录/视频/.rrd | transcripts/*/actions/*/wire/*.jsonl + PNG blobs |

## 待用户输入（阻塞项）

1. **智谱 API key**（GLM-4.6V）：`export ZHIPUAI_API_KEY=...` 后即可跑 `eval_bowl.py --embodiment cubepick --model glm-4.6v --base-url https://open.bigmodel.cn/api/paas/v4 --key-env ZHIPUAI_API_KEY`。
2. **OpenAI API key**（gpt-6-astra，建议 $50 额度）：Day4 前到位即可，先 Kaggle Secrets。
3. **Kaggle 账号跑 Phase 2 notebook**（T4 ×2 + Internet，需手机验证）：逐 cell 粘贴 `kaggle_recon_and_setup.py`，把 CELL 1/5/6 输出发回。

## Phase 1b 补记（9/14 晚）：智谱 API 端点摸底 + GLM-5.3-flash 全链路通过

用户提供了智谱 key（存于本机非 git 目录 `gpt6astra-repro/.env`，不入库）。**关键发现：额度不在标准端点上，在 coding 专用端点**（与 ZCode 同源；ZCode 本机配置实证：`~/.zcode/v2/config.json` 中为 `/api/anthropic` Anthropic 兼容端点）。端点×模型实测矩阵（E1，`test_glm_vision.py` 探测）：

| 端点 | 模型 | 文本 | 图像 | 工具调用 | 备注 |
|---|---|---|---|---|---|
| `open.bigmodel.cn/api/paas/v4`（标准） | glm-4.6/4.7/5.3 | ✅ | ❌(1210 仅 text) | — | 全部 429 余额不足 |
| 同上 | glm-4v-flash | ✅ | ✅（答对颜色） | ❌（不发起调用） | 免费可用但工具弱；max_tokens≤1024 |
| 同上 | glm-4.5v | 429 | — | — | ID 有效但无额度 |
| **`open.bigmodel.cn/api/coding/paas/v4`（coding）** | **glm-5.3-flash** | **✅** | **✅（答对颜色）** | **✅（move_by 参数正确）** | **用户指定，Phase 1b 主配置** |
| 同上 | glm-4.6 | ✅ | ❌（仅 text） | ✅ | coding 计划文本主力 |
| `open.bigmodel.cn/api/anthropic`（Anthropic wire） | glm-4.6 | ✅ | ⚠️ 假通过（答错颜色，图像未进模型） | — | 不可用于视觉 |
| `api.z.ai/api/coding/paas/v4` | glm-4.6 | ✅ | — | — | 备选 |

**Phase 1b 结果（E1）**：`glm-5.3-flash` @ coding 端点驱动 CubePick mock，agent 参数全对齐 RoboCurve（20 调用预算/25% 限速/medium effort），**3 trials 3 成功**（seed 1000–1002），平均 episode_length≈14.3 步；transcript 显示模型逐步读取 `state[eef_pos]/[cube_pos]`、沿对角线 8 次 `move_by` 逼近目标，note 为人类可读的观察-决策记录——与 RoboCurve transcript 形态一致。20 trials 正式基线另行在跑。

对 Isaac 阶段的含义：`Isaac-Lift-Cube` 本体状态不含物体位姿（只有 joint/eef/gripper），**必须依赖视觉** → glm-5.3-flash 的视觉能力是刚需（已验证可用）；若实测不稳，备选 glm-4.6+coding 端点跑文本模式不可行（Isaac 观测无物体坐标），需换视觉模型或给 embodiment 加物体位姿状态字段。

## 文件索引

- 工作目录 `gpt6astra-repro/`（本机，不入库）：`quickstart.py`、`run_mock_agent.py`、`kaggle_recon_and_setup.py`、`eval_bowl.py`、`logs-mock/`
- 源码锚点：`inspect-robots/plugins/inspect-robots-agent/src/inspect_robots_agent/{policy,_llm,_tools}.py`、`plugins/inspect-robots-isaacsim/src/inspect_robots_isaacsim/embodiment.py`、`scripts/boot_proof.py`
