# P3 R2.1/R3 验收与 R4–R7 最终收口计划

日期：2026-09-01  
本次反馈包：`P3_R21_R3_isaac_d20_10s_v1.zip`  
反馈包 SHA256：`2fea92f341557855fabf9e346754d81fc929baae1190d6ac7162bd0bc860f864`

---

## 1. 验收结论

本次 Isaac 20 ms、10 秒正式仿真通过科学运行验收，可以作为 P3 正式 D20 轨迹保留，无需重跑。

当前状态定义为：

```text
R2_DELAY_SEMANTICS_PASS
R3_SCIENTIFIC_RUN_ACCEPTED
R4_FINAL_VALIDATOR_PENDING
```

下一步停止新增仿真，进入纯离线 R4：整理自包含的四条件输入，编写真正 fail-closed 的 `p3_validate.py`，执行八个真实负测试。R4 反馈通过后，再连续完成 R5 指标媒体、R6 证据索引和 R7 最终报告。

---

## 2. 本次已经确认的证据

### 2.1 包和哈希

- ZIP CRC 通过；
- `SHA256.txt` 共 24 条，全部复算通过；
- 正式 runner SHA256：`de872655f1efe5a6ec46905b465e062c91c51da78d55f0392589276d8e5ee130`；
- instrumented actuator SHA256：`85a06b932e91dff057a7f2f4ddcb2697b7bc321ba43deb5c4236680245da5582`；
- Isaac URDF SHA256：`c2aa383744d89c888e38feeb9e52e3723ed6e288ea91c7ecff950cbd711ed8eb`；
- policy、motion、canonical 哈希与 D00 一致。

### 2.2 0 ms 回归

R2 runner 的 0 ms、2 秒轨迹与旧 Isaac D00 前 100 周期比较：

```text
q/q_des/action/root/body position/body orientation/body velocity max diff = 0.0
requested == effective max diff = 0.0
```

因此旧 Isaac D00 10 秒基线可以继续复用。

### 2.3 20 ms 正式运行

正式 trace：

```text
cycles = 500
simulation time = 10 s
fall_reason = null
done 全部为 0
policy_step = 0..499
time_step = 0..499，逐周期 +1
所有浮点字段 finite
```

延迟实测：

```text
唯一 control shift = 1
唯一 physics shift = 4
physics shift max diff = 0.0
4 × 0.005 s = 20 ms
首 4 个 effective target 等于首个 requested target
```

执行器接线：

```text
5 个 actuator groups：legs/feet/waist/waist_yaw/arms
每组每控制周期 compute count = 4
29 个关节每周期 coverage = 1
physics-step index = 1..2000，连续无断点
requested_substep == q_des，max diff = 0.0
```

正式 trace 前 100 周期与 R1 probe 对应字段逐点一致，max diff 为 0.0。

### 2.4 视频

```text
resolution = 1280×720
frames = 250
fps = 25
duration = 10.0 s
```

首、中、末三帧均非黑，全身可见。

---

## 3. 当前不需要重跑仿真的原因

现有正式 trace 已直接证明：

1. q_des 进入显式 per-physics-step position-target FIFO；
2. FIFO 输出实际进入闭环，0 ms 与 20 ms 从第 2 个控制周期开始分叉；
3. 29 个关节接线完整；
4. 延迟为唯一可识别的 4 physics steps；
5. 正式 episode 完整运行 500 周期；
6. 输入资产和核心 runner/actuator 哈希完整。

剩余问题可以通过整理文件、补充依赖哈希和离线 validator 解决。修改已执行 runner 或 actuator 会改变源码哈希，反而破坏当前 trace 的 provenance。因此冻结本次源码，不在原文件上继续编辑。

---

## 4. R4 前需要处理的证据包问题

### P0-1：当前反馈包不能独立重放两个 gate 脚本

`run_delay_gate_r2.py` 仍会读取包外内容：

```text
01_delay_gate_r1/isaac_integration_probe_r1/
03_runs/mj_elbow_d20_10s/summary.json
```

本次 ZIP 没有包含上述 R1 probe 和 MuJoCo summary。

`run_zero_regression_r2.py` 需要：

```text
00_inputs/isaac_d00_reference/policy_trace.npz
00_inputs/isaac_d00_reference/run_manifest.yaml
```

本次 ZIP 也没有包含它们。

处理方式：R4 最终包必须把四个正式条件和回归引用完整复制进同一根目录。最终 validator 禁止使用 R1/R2 fallback 和绝对路径。

### P0-2：FIFO 单测名称与正式实现不一致

正式 Isaac R3 使用：

```text
position_delay_backend=explicit_deque
```

当前 `test_fifo()` 测试的是 Isaac Lab 原生 `DelayBuffer`。R4 应增加针对正式 explicit deque 算法的单测。原生 DelayBuffer 测试可以保留为工程补充，不能作为正式 R3 实现的直接证据。

### P0-3：最终 checkpoint 需要比较哈希值

当前 `checkpoint_100.json` 的 `hashes_ok` 只检查三个哈希是否非空。实际哈希均正确，所以 R3 trace 可以保留。R4 validator 必须比较完整 64 位哈希和实际文件内容，不能只检查非空或八位前缀。

### P0-4：四条件输入尚未放入同一个自包含目录

R4 需要同时包含：

```text
Isaac D00 10 s
Isaac D20 10 s R2
MuJoCo elbow-matched D00 10 s
MuJoCo elbow-matched D20 10 s
Isaac R2 0 ms 2 s 回归
MuJoCo 0 ms 回归证据
```

所有条件必须由一个相对路径 evidence index 指向。

### P1-1：源码注释仍描述了原生 DelayBuffer

执行过的 actuator 源码开头仍有“通过 DelayBuffer.compute”和“DelayBuffer 输出后”等旧注释。正式代码实际使用 explicit deque。

处理方式：

- 保留执行源码原样；
- 在 `KNOWN_LIMITATIONS_R3.md` 记录注释与实现的差异；
- 报告统一写“显式 per-physics-step position-target FIFO”；
- 不把该实验解释为 Isaac 原生 DelayBuffer 的性能验证。

### P1-2：`reset_count=0` 是 runner 常量

最终 validator 不直接信任该字段。无 reset 由 trace 独立推断：

```text
policy_step 连续 0..499
time_step 连续 0..499
actuator physics-step index 连续 1..2000
done 全 0
```

最终字段写为：

```text
reset_observed=false
reset_evidence=independent_trace_counters
```

### P1-3：14-body 顺序没有写进 R3 manifest

创建 `00_inputs/body_mapping.json`，内容至少包括：

```text
anchor_body_name=pelvis
body_names 共 14 个
pelvis 为 index 0
body mapping 来源文件和 SHA256
```

同时复制本次运行使用的：

```text
source/.../l7_29dof_tracking_env_cfg.py
```

validator 检查 body mapping 与源文件一致。该 sidecar 只解释已有 trace，不修改 trace。

### P1-4：仓库为 dirty 状态

`repo_state.json` 记录 Humanoid Lab dirty=true、88 个变动项。最终结论依赖 artifact hash，不依赖 clean commit 假设。

补建 `00_inputs/runtime_dependency_ledger.json`，至少绑定：

```text
frozen_run_isaac_delay_r2.py
p3_instrumented_delayed_actuator_r2.py
scripts/experiments/run_mujoco_onnx.py
deploy/src/era_rl_controller/configs/mimic_dance_9.yaml
l7_29dof_tracking_env_cfg.py
tracking_env_cfg.py
robots/actuator.py
robots/era_l7_29dof.py
Isaac URDF
MuJoCo XML
policy ONNX
dance_9 motion
canonical initial state
repo_state.json
```

每项记录相对路径、SHA256、role 和是否直接参与 R3 runtime。

---

## 5. R4：构建自包含最终门禁包

### Step R4.1：冻结 R3

创建：

```text
13_p3_delay20/03_runs/isaac_d20_10s_r2_frozen/
```

保存原 ZIP 和 SHA256，写 `STATUS.md`：

```text
status=R3_SCIENTIFIC_RUN_ACCEPTED
rerun_required=false
final_validator_pending=true
```

不得修改以下文件：

```text
policy_trace.npz
run_manifest.yaml
checkpoint_100.json
frozen_run_isaac_delay_r2.py
p3_instrumented_delayed_actuator_r2.py
```

### Step R4.2：建立最终目录

建议结构：

```text
13_p3_delay20/
├── 00_inputs/
│   ├── canonical_initial_state.npz
│   ├── policy/
│   ├── motion/
│   ├── isaac_model/
│   ├── mj_elbow_model/
│   ├── executed_sources/
│   ├── runtime_dependencies/
│   ├── body_mapping.json
│   ├── repo_state.json
│   ├── runtime_dependency_ledger.json
│   └── p3_condition_index.json
├── 01_delay_gate_r2/
│   ├── isaac_0ms_regression_r2/
│   └── explicit_fifo_unit_test.json
├── 03_runs/
│   ├── isaac_d00_10s_reference/
│   ├── isaac_d20_10s_r2/
│   ├── mj_elbow_d00_10s_reference/
│   └── mj_elbow_d20_10s/
├── 04_analysis/
│   ├── p3_validate.py
│   └── p3_final_gate.json
├── 06_checks/
│   ├── run_p3_negative_tests.py
│   └── p3_negative_tests.json
├── KNOWN_LIMITATIONS_R3.md
├── README_R4.md
└── SHA256.txt
```

### Step R4.3：条件索引

`p3_condition_index.json` 为四个正式条件分别记录：

```text
engine
delay_requested_ms
trace relative path
manifest relative path
summary relative path
video relative path or null
policy/motion/canonical/model/runner/actuator hashes
baseline reuse status
zero-regression evidence relative path
```

禁止 `E:/...`、`/mnt/e/...` 和目录搜索 fallback。

Isaac 和 MuJoCo 的 D00/D20 runner hash 允许不同，前提是对应 0 ms 回归证据通过且被 validator 验证。

### Step R4.4：实现 `p3_validate.py`

接口：

```bash
python 04_analysis/p3_validate.py --root <解压后的13_p3_delay20根目录>
```

validator 必须只使用 `--root` 下的相对路径，从原始文件重新计算。

#### A. 文件和哈希门禁

```text
四条件 trace/manifest/summary 全部存在
condition index 没有绝对路径
SHA256 与文件内容一致
policy/motion/canonical 跨四条件一致
同一引擎 D00/D20 model 一致
Isaac model = c2aa3837...d8eb
MJ model = ab1e1862...e7f9
executed runner/actuator 与 manifest 哈希一致
runtime dependency ledger 全部可复算
repo_state 与 D20 manifest 内嵌内容一致
```

#### B. trace 结构门禁

```text
每个正式条件恰好 500 cycles
policy_step=0..499
time_step=0..499
t 从 0 开始、control dt=0.02
所有 required float fields finite
q/dq/q_des/action/root/body exact shape
done 全 0
fall_reason 为空
summary 与 trace 一致
```

#### C. Isaac D20 延迟门禁

```text
requested_substep shape=(500,4,29)
effective_substep shape=(500,4,29)
compute_count_by_group shape=(500,5)，全部为4
coverage shape=(500,29)，全部为1
owner 映射全程一致
physics index=1..2000
physics_time == physics_index×0.005，允许 float32 误差 <=1e-6
requested_substep == q_des，max diff <=1e-6
唯一 physics shift=4
shift max diff <=1e-6
首4步 repeat-first
manifest measured 字段为 null/NOT_EVALUATED
validator 输出 measured=4 steps/20 ms
```

唯一 shift 的搜索范围至少为 0–20 physics steps。最优 shift 需要满足阈值，第二优 shift必须明显不满足阈值。

#### D. MuJoCo D20 延迟门禁

```text
physics dt=0.002
每 control 10 physics steps
唯一 physics shift=10
control shift=1
repeat-first
fifo_used=true
```

#### E. 基线配对门禁

```text
Isaac R2 0 ms vs Isaac D00 前100周期全字段 max diff <=1e-6
MuJoCo 新0 ms vs MuJoCo D00 前100周期 max diff <=1e-6
D00/D20 start_frame/seed/gain/history/obs source/canonical 一致
body mapping 一致
```

#### F. checkpoint 门禁

重新计算正式 D20 前 100 周期，比较 `checkpoint_100.json` 中每个字段。`hashes_ok` 由 validator 使用完整哈希重新定义。

#### G. 输出状态

只允许：

```text
VALID_P3
INVALID_ARTIFACT_HASH
INVALID_BASELINE_PAIRING
INVALID_DELAY_IMPLEMENTATION
INVALID_TRACE
INVALID_P3
```

合法结果：

```text
decision=VALID_P3
validity_pass=true
exit=0
```

其他状态均非零退出。

### Step R4.5：八个真实负测试

每个 case 必须：

1. 把真实 R4 根目录复制到临时目录；
2. 只修改一个文件或字段；
3. 调用同一个 `p3_validate.py --root <temp>`；
4. 检查非零退出和预期 reason；
5. 保存实际命令、exit code、stdout/stderr 和 reason。

八项建议：

```text
T1 修改一个 effective target，期望 INVALID_DELAY_IMPLEMENTATION
T2 删除 q_des_effective_substep，期望 INVALID_TRACE
T3 把 requested_delay_ms 改为 15，期望 INVALID_DELAY_IMPLEMENTATION
T4 修改 canonical hash，期望 INVALID_ARTIFACT_HASH
T5 修改 model hash，期望 INVALID_ARTIFACT_HASH
T6 删除一个 control cycle，期望 INVALID_TRACE
T7 破坏 physics-step index 连续性，期望 INVALID_DELAY_IMPLEMENTATION
T8 修改 D00/D20 seed 或 start_frame，期望 INVALID_BASELINE_PAIRING
```

通过条件：

```text
8/8 validator 返回非零
8/8 reason 与预期一致
没有 mock validator
```

---

## 6. R4 下一次反馈节点

R4 完成后先反馈，不要直接改最终报告。

反馈包建议命名：

```text
P3_R4_final_gate_v1.zip
```

必须包含：

```text
完整自包含 00_inputs
四条件 trace/manifest/summary
p3_condition_index.json
runtime_dependency_ledger.json
body_mapping.json
p3_validate.py
p3_final_gate.json
run_p3_negative_tests.py
p3_negative_tests.json
八个 case 的 stdout/stderr 摘要
README_R4.md
KNOWN_LIMITATIONS_R3.md
SHA256.txt
```

`README_R4.md` 开头回答：

```text
final validator 是否从解压包独立运行
decision 是否为 VALID_P3
Isaac measured delay 是否为 4 steps/20 ms
MuJoCo measured delay 是否为 10 steps/20 ms
四条件是否均为500周期、无fall
0 ms baseline pairing 是否通过
negative tests 是否8/8
是否还有绝对路径或 fallback
```

---

## 7. R5：R4 通过后的统计与媒体

R4 通过后，不再进行新仿真，直接从四条件 trace 计算。

### 7.1 指标

至少输出：

```text
survival/failure/reset evidence
29-joint q trajectory sensitivity
legs/waist/arms 分组 q sensitivity
q_des_requested-q tracking RMS
q_des_effective-q tracking RMS
root height/roll/pitch
14-body position/orientation/linear velocity/angular velocity
pelvis anchor position/orientation
Isaac-MuJoCo gap：D00 vs D20
MuJoCo torque RMS/peak
Isaac torque=UNAVAILABLE
```

四元数误差使用：

```text
2*acos(clip(abs(dot(q1,q2)),0,1))
```

不要直接对四元数四个分量做普通 RMSE 后称为 orientation error。

### 7.2 已独立复算的预期数值

以下数字用于交叉检查，最终以 R5 脚本输出为准：

| 指标 | Isaac | MuJoCo |
|---|---:|---:|
| D20 vs D00 q sensitivity | 0.030929 rad | 0.048023 rad |
| D20 vs D00 q_des sensitivity | 0.033234 rad | 0.049301 rad |
| D20 vs D00 body position sensitivity | 0.026946 m | 0.051533 m |
| D20 vs D00 body orientation sensitivity | 0.052106 rad | 0.104533 rad |
| D00 requested tracking RMS | 0.131353 rad | 0.122301 rad |
| D20 requested tracking RMS | 0.145288 rad | 0.139712 rad |
| D20 effective tracking RMS | 0.130011 rad | 0.123062 rad |

跨引擎预期：

| 指标 | D00 | D20 | 变化 |
|---|---:|---:|---:|
| q gap | 0.055946 rad | 0.067324 rad | +20.3% |
| body position gap | 0.104610 m | 0.074709 m | -28.6% |
| body orientation gap | 0.104374 rad | 0.150575 rad | +44.3% |

预期解释：20 ms 后，两侧都能继续存活。requested tracking error 增加，effective tracking 基本保持；策略请求与实际执行目标之间出现相位差。MuJoCo 的轨迹敏感度高于 Isaac。跨引擎关节和姿态差距扩大，body position gap 缩小，呈现指标 tradeoff。

### 7.3 图表和视频

重新生成：

```text
q D00/D20 两引擎对比
requested/effective target 对比
requested/effective tracking
root 指标
14-body position/orientation
分组 q sensitivity
跨引擎 gap D00/D20
survival timeline
MuJoCo torque
四条件汇总表
```

四格视频只能使用正式 10 秒来源。每格标注：

```text
engine
delay
physics dt
control dt
model variant
```

输出 `media_manifest.json`，记录每个格子的来源视频和 SHA256。

---

## 8. R6：P0/P2 证据索引收口

按照整体计划完成：

1. R3 接口门禁证据并入最终包；
2. 14-body/anchor 指标进入最终表；
3. Isaac commit/dirty state和 runtime dependency ledger 进入 provenance；
4. 报告明确区分接口 G0 与资产 G0；
5. P2 validator 删除绝对路径 fallback；
6. 验证 `elbow_recomputed == audit.elbow_xml_sha256`；
7. P2 已有科学 trace 不重跑。

---

## 9. R7：更新三项目最终报告

沿用既定主线：

1. 三个项目的 Sim2Sim 实现方式；
2. 关键代码和接口；
3. 实验设计目的；
4. 接口、资产、armature、延迟四层实验；
5. 实验中发现的问题和纠偏过程；
6. N5-R2、P1/P2、P3 的有效结果；
7. 能支持的结论和适用范围；
8. Sonic 后续建议和 Sim2Sim 经验。

P3 结论限定在：

```text
dance_9
seed=42
10 s
gain=1.0
当前 policy/motion/canonical/model
显式 position-target FIFO
```

不要外推到所有动作、所有延迟值或 Isaac 原生 DelayBuffer。

最终同时输出一页摘要，核心图包括：

- 三项目实现对比；
- armature 隔离结果和全身迁移；
- 0/20 ms 下 requested/effective、tracking 和跨引擎 gap。

---

## 10. 当前总路线

```text
R3 正式运行已接受，不重跑
        ↓
R4 自包含输入 + p3_validate + 8 个真实负测试
        ↓ 反馈一次
R5 完整指标、图表、正式四格视频
        ↓
R6 P0/P2 证据索引收口
        ↓
R7 更新三项目报告和一页摘要
        ↓
原两日计划基础交付正式关闭
```

继续暂停：

```text
WAIST_ONLY
N6/17关节消融
gain 大矩阵
frictionloss/dt/solver 新机制
Gym 50 Hz
其他新动作和新延迟档位
```

