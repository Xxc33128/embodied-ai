# P3 R5 验收结论与 R5.1–R7 最终收口执行清单

日期：2026-09-02  
本次反馈包：`P3_R5_metrics_media_v1.zip`  
适用 Win 工作根目录：`E:\sim2sim-week-2026-08-26`  
继承计划：`2026-09-02-P3-R4终验通过与R5-R7最终执行清单.md`

本文把反馈包中的说明视为待核验声明。验收结论来自新鲜解压、R4+R5 overlay 独立复跑、源码检查、指标复算和图片抽查。

---

## 1. 总结论

R5 的科学结果通过验收：

```text
R4: VALID_P3
R5: VALID_R5
R5_SCIENTIFIC_RESULT_ACCEPTED
```

四条正式 trace、主指标和结论均可保留。Isaac 和 MuJoCo 仿真不再运行，四条 trace、模型、runner、actuator、checkpoint 全部冻结。

最终报告前增加一个纯离线的 `R5.1 展示与元数据补丁`，修复单位、anchor orientation、两张易误读图和媒体时间口径。R5.1 不改变已接受的主指标，也不重新编码仿真结果。

后续路线：

```text
R5 科学结果已接受
  ↓
R5.1 展示/元数据补丁（预计 30–60 分钟，无仿真）
  ↓
R6 最终证据索引与 P0/P2 validator 收口
  ↓
R7 更新三项目最终报告和一页摘要
  ↓
提交最终包，关闭原两日计划基础交付
```

下一次反馈放在 R5.1、R6、R7 全部完成后。无需为 R5.1 单独再发一轮小包。

---

## 2. 独立验收记录

### 2.1 包级检查

| 项目 | 结果 |
|---|---|
| R5 ZIP SHA256 | `4f0c55f6725289fb82c6085a8b2ef83543f21fe67a39f97d947e4c0cb7cbe1d7` |
| ZIP CRC | PASS |
| R5 小反馈包 payload | 32 项 + `SHA256.txt` |
| 小包 SHA 校验 | 32/32 PASS |
| R4+R5 overlay 运行 `p3_validate.py` | `VALID_P3`，A–F 全 PASS |
| R4+R5 overlay 运行 `p3_summarize_r2.py` | `VALID_R5`，0 mismatch |
| 四 trace SHA | 全部匹配冻结值 |
| 500 cycles / 12·3·14 groups / D00 target equality | PASS |
| first/mid/last 抽帧 | 1280×720、可见、非黑 |
| 四格输出 SHA256 | `0b626d2a92cbe73b4b6b02e87f8e583928cba3ec74a5ce43e8ae887b6ad4f77b` |

Win 完整树的“101/101”与 R5 小反馈包的“32/32”属于两个哈希范围。最终交付中应分别命名，禁止都称为 `SHA256.txt 101 项`：

```text
SHA256_FULL_TREE.txt       # 完整 R4+R5 或最终树
SHA256_FEEDBACK_PACKAGE.txt # 小反馈包
```

### 2.2 已接受的 R5 主结果

以下结果已独立复算，可以进入最终报告：

| 指标 | Isaac | MuJoCo |
|---|---:|---:|
| D20 vs D00 q sensitivity | 0.030929 rad | 0.048023 rad |
| q_des sensitivity | 0.033234 rad | 0.049301 rad |
| world body sensitivity | 0.026946 m | 0.051533 m |
| pelvis-relative body sensitivity | 0.009635 m | 0.017089 m |
| body orientation sensitivity | 0.052106 rad | 0.104533 rad |
| D00 requested tracking | 0.131353 rad | 0.122301 rad |
| D20 requested tracking | 0.145288 rad | 0.139712 rad |
| D20 effective tracking | 0.130011 rad | 0.123062 rad |

跨引擎结果：

```text
q gap                         0.055946 → 0.067324 rad  (+20.3%)
world body position gap       0.104610 → 0.074709 m    (-28.6%)
pelvis-relative body gap      0.017400 → 0.023317 m    (+34.0%)
body orientation gap          0.104374 → 0.150575 rad  (+44.3%)
pelvis position gap           0.103292 → 0.070367 m    (-31.9%)
pelvis orientation gap        0.040751 → 0.084816 rad  (+108.2%)
```

保留结论：

1. 两侧在当前 `dance_9 / seed 42 / 10 s / gain 1.0` 条件下均完成 20 ms 延迟运行；
2. MuJoCo 的关节和姿态轨迹敏感度高于 Isaac；
3. requested tracking 上升，effective tracking 接近各自 D00，支持执行目标相移解释；
4. world body gap 的下降主要来自 pelvis/root 平移更接近；
5. pelvis-relative body gap 和 orientation gap 同时增加，整体结果呈现 tradeoff；
6. 结论不外推到其他动作、其他延迟、Isaac 原生 DelayBuffer 或其他模型资产。

---

## 3. R5.1 必须修复的项目

### 3.1 每个 CSV 指标必须使用确定单位

当前 `p3_metrics_long.csv` 只出现：

```text
rad/m mixed
mixed
N*m
```

这会使最终索引和报告无法自动判断每一行的物理意义。修改 `p3_summarize_r2.py`，建立显式 unit map：

| 指标类型 | 单位 |
|---|---|
| q、q_des、tracking、roll、pitch、orientation | `rad` |
| body/root/anchor position | `m` |
| body linear velocity | `m/s` |
| body angular velocity | `rad/s` |
| torque RMS/peak | `N·m` |
| saturation fraction | `1` |
| survival | `cycle` 或 `s` |
| policy action variation | `policy_action_unit` |
| q_des variation | `rad` |

验收要求：

```text
[ ] p3_metrics_long.csv 中不存在 mixed、rad/m mixed
[ ] 每一行都有非空 unit
[ ] p3_metric_schema.json 不使用 unit=mixed
[ ] nested anchor/body 指标也展开进入 long CSV
```

`p3_metrics_wide.csv` 当前实际为三列 long/tidy 结构。二选一处理：

1. 重命名为 `p3_metrics_engine_tidy.csv`；或
2. 真正改成一行一个 metric、Isaac/MuJoCo 分列的 wide 表。

推荐方案 1，减少无意义机械改写。

### 3.2 补 feet/hands anchor orientation

当前 JSON 有 pelvis/ankles/wrists 的 position gap，orientation 只有 pelvis。补充：

```text
anchor_orientation_gap:
  pelvis
  left_ankle_roll_link
  right_ankle_roll_link
  left_wrist_roll_link
  right_wrist_roll_link
```

每个 anchor 分别计算 D00 和 D20：

```text
RMS(2*acos(abs(dot(q_isaac_normalized, q_mj_normalized))))
```

输出到：

```text
p3_metrics.json
p3_metrics_long.csv
p3_metric_schema.json
P3_result_summary.md
```

保持 position 与 orientation 分栏，禁止把米和弧度合成一个 “anchor gap”。

### 3.3 替换图 08

当前 `08_body_orientation_geodesic.png` 画的是 robot-vs-motion。代码和摘要已经声明 MuJoCo reference body 的 frame/COM 语义未完成同口径对齐，这张图容易被误读为有效的跨引擎任务误差。

图 08 改成两栏的有效比较：

```text
左：每个 cycle 的 D20-vs-D00 14-body orientation geodesic RMS
    两条线：Isaac、MuJoCo

右：每个 cycle 的 Isaac-vs-MuJoCo 14-body orientation geodesic RMS
    两条线：D00、D20
```

两栏单位均为 rad，全部来自 `actual_body_quat_w(14,4)`。图名保持：

```text
08_body_orientation_geodesic.png
```

robot-vs-motion 诊断量可以保留在 JSON，字段名必须带 `diagnostic_unaligned`，不进入主图和结论。

### 3.4 修图 09 和图 11 的混合单位问题

图 09 左侧把 q/body position/body quaternion 的绝对值放在同一数轴，单位同时包含 rad 和 m。改为：

- 左侧使用 4 个小面板，每个面板只画一个指标并显示自己的单位；或
- 左侧改成带数值和单位的表格，右侧保留相对变化百分比。

推荐 4 个小面板。

图 11 当前把米和弧度作为同一组绝对柱高比较。改成无量纲：

```text
MuJoCo sensitivity / Isaac sensitivity
```

五个指标分别为 q、q_des、body world、body pelvis-relative、body orientation，增加 `ratio=1` 参考线。标题明确写 `dimensionless engine sensitivity ratio`。

图 10 建议拆成两个面板：

- RMS：applied/raw/qfrc；
- peak 和 saturation fraction 分开显示。

这项为推荐修正，不影响 R5 validity。

### 3.5 给所有 plot data CSV 增加标签和单位

当前 `data_02`、`data_07`、`data_09`、`data_11` 只有数值行，缺少 metric label。

统一格式：

```csv
metric,D00,D20,unit,aggregation,coordinate_frame
...
```

分组图可使用：

```csv
group,isaac,mujoco,unit,n_joints
ALL29,...
LEGS12,...
```

每张图应能只靠对应 CSV 复现类别和单位，不依赖阅读 Python 源码猜测行顺序。

### 3.6 明确视频的两种时间

`media_manifest.json` 当前显示：

```text
Isaac D00: 250 frames / 30 fps = 8.33 s container playback
MJ D00/D20: 295 frames / 30 fps = 9.83 s container playback
```

四个源都来自 500-cycle、10 秒正式仿真。最终四格视频把完整帧序列按索引均匀映射到 10 秒，并以 25 fps 输出 250 帧。这个处理可保留，manifest 必须区分：

```json
{
  "container_frame_count": 250,
  "container_fps": 30.0,
  "container_playback_duration_s": 8.333333,
  "covered_simulation_cycles": 500,
  "covered_simulation_time_s": 10.0,
  "frame_to_sim_time_mapping": "uniform index-proportional assumption",
  "has_per_frame_sim_timestamp": false
}
```

最终视频中的 `t=...` 标为 `nominal simulation time`。在 `visibility_note` 中补充：

```text
视频只用于定性观察；源视频没有逐帧仿真时间戳，画面通过完整帧序列按索引映射到已接受的 10 秒运行。所有定量结论来自冻结 trace。
```

不需要重跑仿真。若脚本更新后最终视频画面和标签不变，可以保留已有 MP4，只重建 manifest 和抽帧；若标签文本改变，则离线重新编码一次。

### 3.7 两个代码卫生补丁

在 `p3_summarize_r2.py` 中：

1. 删除重复读取 `p3_condition_index.json` 的一行；
2. finite 检查加入：
   - `dq(29)`；
   - D20 `q_des_requested(29)`、`q_des_effective(29)`；
   - body position/quaternion/linear velocity/angular velocity；
   - MuJoCo torque 字段。

survival/fall/reset 字段从 upstream gate、trace、manifest 读取并交叉验证，禁止只在输出构造时硬编码 `None/False/500`。

---

## 4. R5.1 验收方式

R5.1 在完整 R4+R5 树执行：

```bash
python 04_analysis/p3_validate.py --root . --output <temp>/p3_gate_recheck.json
python 04_analysis/p3_summarize_r2.py --root .
python 05_media/make_four_panel_r5.py --root .
```

验收结果必须为：

```text
p3_gate_recheck.decision = VALID_P3
p3_r5_validation.decision = VALID_R5
crosscheck.mismatches = 0
四 trace SHA 与 R4 pin 完全一致
```

额外检查：

```text
[ ] long CSV 没有 mixed unit
[ ] anchor position/orientation 五个 anchor 均存在
[ ] 图 08 不再使用未对齐 robot-vs-motion 作为主图
[ ] 图 09 绝对值面板不共用混合单位数轴
[ ] 图 11 为无量纲 ratio
[ ] plot CSV 有 metric 和 unit
[ ] media manifest 区分 container time 与 simulation time
[ ] first/mid/last 可见
[ ] 完整树 SHA 全部通过
```

R5.1 完成后直接进入 R6，无需再次申请启动权限。

---

## 5. R6-1：修复 P2 最终 validator 的两个已知缺口

输入包使用：

```text
P2_fullbody_arm_only_v1_1.zip
SHA256=a48749cb4513df25c1a74f6d52bd803f6c97e15ab7c7d8cbb290bfab4095398a
```

现有科学 trace 和结论 `NO_INCREMENTAL_ARM_GROUP_EFFECT` 保留，不运行新仿真。

### 5.1 删除绝对路径 fallback

以下两个文件仍含允许外部文件替代包内资产的 fallback：

```text
01_asset/audit_fullbody_arm_assets.py
04_analysis/p2_validate.py
```

强制只使用：

```text
00_inputs/p1_elbow_only/l7_29dof_neck_fixed_elbow_matched.xml
```

该文件缺失时直接：

```text
decision=INVALID_P2
reason=PACKAGED_ELBOW_XML_MISSING
exit 1
```

不得尝试 `/mnt/e/...` 或 `E:/...`。

### 5.2 把 elbow 哈希真正纳入 validity

当前 validator 会计算 `elbow_recomputed`，但 `asset_ok` 没有要求它等于 audit 中的哈希。增加：

```text
elbow_recomputed == audit["elbow_xml_sha256"]
```

同时要求：

```text
audit["elbow_xml_sha256"] == ab1e1862367be20a91edeefca12c6faedbc3e99a151d662cf42b4c56da98e7f9
```

增加至少两个真实负测试：

1. 删除 packaged elbow XML；
2. 修改 packaged elbow XML 一个字节。

两项均调用正式 `p2_validate.py`，必须得到 `INVALID_P2 / exit 1`。修补后用现有 trace 重算，科学 decision 应保持：

```text
NO_INCREMENTAL_ARM_GROUP_EFFECT
```

---

## 6. R6-2：重构 P0 evidence closure validator

当前 P2 包内的：

```text
00_inputs/p0_evidence_closure/06_checks/validate_evidence_closure.py
```

仍使用 Win/WSL 绝对路径候选和隐式 fallback。改成显式接口：

```bash
python validate_evidence_closure.py \
  --root <overlay_root> \
  --source-root <source_zip_directory> \
  --mode embedded|full-provenance
```

规则：

### embedded

- 只验证 overlay 内部文件；
- 验证 G0-I、G0-A、body/anchor、环境信息、内部哈希；
- 不读取外部绝对路径；
- 外部 ZIP 不在场时输出 `EXTERNAL_SOURCE_NOT_CHECKED`；
- 不得把 embedded 结果称为 full-provenance PASS。

### full-provenance

- 从 `--source-root` 定位 source ZIP；
- 对源 ZIP 实际重算 SHA256；
- 验证 master index 中的包内相对路径；
- 任一源缺失或哈希不符时 fail closed。

已知源包交叉检查值：

| Artifact | SHA256 | Payload |
|---|---|---:|
| B31 基础包 | `2f4e8c15fdb48a1dfd46df6407a06d68a26ebbc3a0ae9c3e5df1ba3d29e9e985` | 213 |
| R3 interface gate | `c1a1011b963a2a0917e2178accf4615b71a97aa677f906c8af4b32e946252b3e` | 47 |
| P1 elbow-only | `8bb8ba9658c81160c7d6542e2b418637b7b736688b8a226ba885327b6d8b6824` | 203 |
| P2 ARM_ONLY v1.1 | `a48749cb4513df25c1a74f6d52bd803f6c97e15ab7c7d8cbb290bfab4095398a` | 347 |
| P3 R3 | `2fea92f341557855fabf9e346754d81fc929baae1190d6ac7162bd0bc860f864` | 25 |
| P3 R4 | `9a4ebdf3c75afa72bbf25d108cbce3d75d34eb5fff00aa36aa973493ee64a3e6` | 70 |
| P3 R5 v1 | `4f0c55f6725289fb82c6085a8b2ef83543f21fe67a39f97d947e4c0cb7cbe1d7` | 33 |

这些值用于交叉检查。Win 端最终索引以实际文件重算结果为准。

---

## 7. R6-3：建立最终证据索引

创建独立目录：

```text
14_final_delivery/
├── FINAL_DELIVERY_INDEX.csv
├── FINAL_DELIVERY_README.md
├── FINAL_CLAIM_EVIDENCE_MATRIX.csv
├── validation/
├── report/
└── assets/
```

### 7.1 FINAL_DELIVERY_INDEX.csv

字段：

```csv
artifact_id,artifact,role,relative_path,sha256,payload_count,
validation_command,validation_result,evidence_grade,
required_for_full_provenance,supersedes
```

至少索引：

1. B31 三项目基础包；
2. R3 接口门禁；
3. N5-R2 隔离实验；
4. P1 elbow-only 全身迁移；
5. P2 ARM_ONLY；
6. P3 R3 正式 Isaac 20 ms；
7. P3 R4 门禁；
8. P3 R5.1 指标和媒体；
9. 最终报告；
10. 一页摘要。

### 7.2 FINAL_CLAIM_EVIDENCE_MATRIX.csv

每条最终结论建立唯一 `claim_id`：

```csv
claim_id,claim,scope,evidence_artifact,evidence_path,
metric_or_gate,value,unit,limitations
```

至少包含：

- 三项目 Sim2Sim 实现链；
- G0-I 接口门禁；
- G0-A 资产门禁；
- N5-R2 elbow armature 74.8%；
- P1 elbow-only 全身迁移；
- P2 `NO_INCREMENTAL_ARM_GROUP_EFFECT`；
- P3 `BOTH_SURVIVE_20MS_ON_DANCE9`；
- P3 requested/effective tracking；
- P3 world/pelvis-relative tradeoff；
- Sonic II/IM/MM/MI 证据等级和限制。

---

## 8. R7：最终报告结构

不要直接覆盖 B31 历史报告。复制并生成：

```text
14_final_delivery/report/本周三项目Sim2Sim调研与实验总结_最终版.md
14_final_delivery/report/本周三项目Sim2Sim一页摘要_最终版.md
```

报告采用以下主线：

### 8.1 执行摘要

回答：

- 研究了什么；
- 为什么比较三个项目；
- 本周获得了哪些有效证据；
- 最重要的三条结论；
- 结论适用范围。

### 8.2 三项目分别怎样实现 Sim2Sim

按 Humanoid Lab、Humanoid Gym、Sonic 分三节。每节包含：

1. 训练引擎；
2. 部署/回放引擎；
3. policy 输入和输出；
4. observation history；
5. action→q_des→PD/actuator→physics 的调用链；
6. physics/control dt；
7. 资产格式和关节顺序；
8. 关键代码路径；
9. 当前证据等级和限制。

随后给出三项目对比表。Sonic 严格依据现有 `sonic_II_IM_MM_MI_ledger.csv`，缺失证据保持 unavailable，不补造精确数字。

### 8.3 为什么按五层设计实验

依次解释：

1. 接口 G0-I；
2. 资产 G0-A；
3. 单关节执行器参数隔离；
4. 全身闭环迁移 P1/P2；
5. 0/20 ms 延迟鲁棒性 P3。

明确写出每层控制的变量、测试证据和允许得出的结论。

### 8.4 实验纠偏过程

按因果顺序写：

1. URDF root 世界变换重复写入局部 joint；
2. 等效惯量出现 91 倍/11.5 倍异常；
3. 旧 armature 结论撤回；
4. 新增资产 G0 和 fail-closed gate；
5. N5-R2 恢复出有效局部 armature 因果；
6. P1/P2 检查局部效应向全身闭环迁移；
7. P3 修复 delay target 可观测性、prehistory 和 validator；
8. R5 对 world/pelvis-relative 指标进行分解。

### 8.5 有效结果

按顺序展示：

1. Lab/Gym/Sonic 基础实现和基线；
2. N5-R2；
3. P1 elbow-only；
4. P2 ARM_ONLY；
5. P3 0/20 ms；
6. 三项目经验。

每个数字都应带 claim_id 或相对证据路径。

### 8.6 结论、限制和 Sonic 建议

区分：

- 已被实验支持；
- 当前只观察到；
- 当前不可判断；
- 后续可选实验。

P3 限定到 `dance_9 / seed 42 / 10 s / gain 1.0 / 显式 position-target FIFO / 当前资产 / 0·20 ms`。

### 8.7 附录

包含：

- 最终证据索引；
- 环境和 commit；
- 指标公式；
- 术语表；
- 复现命令；
- 已知限制。

---

## 9. R7 图表选择

最终报告优先使用：

1. 三项目实现对比图或表；
2. N5-R2 elbow 单关节响应；
3. P1/P2 全身迁移对比；
4. P3 requested/effective tracking；
5. P3 world 与 pelvis-relative 分解；
6. P3 orientation/cross-engine gap；
7. 四格视频链接和抽帧。

不要使用 R5 v1 的旧图 08 和旧图 11。只引用 R5.1 修订版。

所有 Markdown 图片使用最终报告目录的相对路径，例如：

```markdown
![P3 body gap 分解](../assets/p3/07_body_position_world_vs_pelvis_relative.png)
```

最终检查：

```text
[ ] 每个图片路径实际存在
[ ] 解压到任意目录后图片仍可显示
[ ] 不引用 E:/、/mnt/e 或 Mac 绝对路径
[ ] 图标题、正文、表格单位一致
[ ] 不使用“不是……而是……”句式
[ ] 不写没有证据路径的精确数字
[ ] 不把 sensitivity 写成性能退化
[ ] 不把单一 world position 下降写成整体一致性提升
```

---

## 10. 一页摘要

一页摘要只保留：

1. 三项目 Sim2Sim 实现对比；
2. 本周实验链：接口→资产→单关节→全身→延迟；
3. 三条核心结果：
   - elbow armature 在有效隔离条件下有明确局部作用；
   - 扩展到全部手臂关节没有获得额外整体收益；
   - 20 ms 下两侧均存活，但关节、姿态和 root/body 指标呈现 tradeoff；
4. 三张核心图；
5. 适用范围和下一步。

不要把调试细节堆入一页摘要。

---

## 11. 最终 validator

创建：

```text
14_final_delivery/validate_final_delivery.py
```

至少验证：

1. `FINAL_DELIVERY_INDEX.csv` 每条路径存在；
2. 所有 SHA256 可重算；
3. full-provenance 模式下源 ZIP 均存在且同哈希；
4. claim matrix 的 evidence path 可解析；
5. 报告中的本地图片全部存在；
6. 报告没有 Win/WSL/Mac 绝对路径；
7. G0-I/G0-A 命名明确；
8. P2 和 P3 validators 均为有效状态；
9. R5.1 long CSV 没有 mixed unit；
10. 最终报告不引用旧 R5 图 08/11；
11. 完整树 SHA 校验通过。

正式命令：

```bash
python validate_final_delivery.py \
  --root . \
  --source-root <source_zip_directory> \
  --mode full-provenance
```

最终输出：

```text
decision=VALID_FINAL_DELIVERY
exit=0
```

至少增加四个真实负测试：

1. 删除一张被报告引用的图片；
2. 修改一个 source ZIP hash；
3. 删除 packaged P2 elbow XML；
4. 把一行 R5 unit 改为 mixed。

四项均应 fail closed。

---

## 12. 最终反馈包

下一次只发送一个最终包：

```text
Sim2Sim_week_final_R5_1_R6_R7_v1.zip
```

建议包含：

```text
14_final_delivery/
R5.1 修订脚本、JSON、CSV、图和媒体 manifest
P2 修订 validator、资产 audit、负测试和重算 gate
P0 evidence closure 修订 validator
P3 R4/R5 validation JSON
完整 FINAL_DELIVERY_INDEX.csv
完整 FINAL_CLAIM_EVIDENCE_MATRIX.csv
最终报告
一页摘要
FINAL_SHA256.txt
validation logs
```

大体积历史 trace 和源 ZIP 可以继续通过索引引用，不重复嵌入最终小包；full-provenance validator 通过 `--source-root` 验证它们。最终包 README 必须列出需要同时交付的源 ZIP 名称和哈希。

---

## 13. 完成定义

同时满足以下条件后，原两日计划基础交付正式关闭：

```text
[ ] R5.1 展示和元数据修正完成
[ ] R5 主数值保持不变，VALID_R5
[ ] P2 validator fail-closed 补丁完成，结论保持 NO_INCREMENTAL_ARM_GROUP_EFFECT
[ ] P0 embedded/full-provenance 两模式完成
[ ] FINAL_DELIVERY_INDEX 和 claim matrix 可验证
[ ] 三项目最终报告完成
[ ] 一页摘要完成
[ ] final validator 输出 VALID_FINAL_DELIVERY
[ ] 最终 ZIP SHA256 通过
```

完成后，WAIST_ONLY、N6、gain 扫描、frictionloss/dt/solver、Gym 50 Hz、其他动作和其他延迟档位全部转入下阶段可选研究，不影响本周交付关闭。

