# 第六轮对抗性审查（聚焦：W6 固定噪声三方 harness，commit 810fcd8）

2026-09-18。只读核查 + 服务器实测（ssh <实验服务器> / gap-repro 容器）。对照 upstream：`policies/policy.py` infer noise 路径、`models_pytorch/pi0_pytorch.py` sample_actions、`models/pi0.py` L223-231；容器内 `/data/pi05_hybrid/openpi` 同段逐行核对（一致）。**本轮无红。VERDICT true/true 的证据链成立，维持。**

## 红

无。第五轮红1（噪声未固定）的修复真实有效：三路确实消费了同一 noise 数组，差值归因（设备/转换）成立。

## 黄

**Y1（黄）结论覆盖：plan §6.3 W6 多项要求未完成且未列入残留，"W6 CLOSED" 名义偏满。**
- 逐张量映射：库内唯一逐张量记录是 LP2 的 `pi05_libero→pi05_libero_pt`（811 张量，另一 checkpoint）；**实际部署的同事转换 `robodojo_pi05_pt` 无逐张量映射/保真记录**（runtime.md 仅记决策）。本次 harness 是行为级验证，不能替代映射记录。
- NaN/Inf：harness 无显式检查（仅 stdout absmax≈1.03 隐含本次输入有限值）；结论与残留均未提。
- 按维门槛：JSON 有 per_dim_mean_absdiff（好），但判定只用两条总体 mean 带（0.01/0.05），无冻结的按维关节/夹爪门槛。
- FK 误差：完全未评估、未列残留。
- 未解码动作（50×32）与关键中间量：计划明确要求，未做（assert 只验解码后 14 维；npz 只存解码后）。
- 结论残留仅两条（npz 逐对齐矩阵、W11 行为级），以上缺口均未登记。

**Y2（黄）阈值预注册成立但依据未记录，且计划要求的校准 fixture 未做。**
- 预注册核实成立：0.01/0.05 判定代码原样在 commit 850c3c9（09:51:27）的 harness 内；该版本 `NOISE_SHAPE=(50,14)` 必然在 torch 路崩溃（同 commit handover 自证阻塞中），不可能产出任何数字；结果 810fcd8 于 11:02:46 提交。即 **门槛先于全部固定噪声数字 71 分钟入库**，好于第五轮的"先出数字后定带"。
- 但：0.01/0.05 的选取依据无任何文字（仅代码注释一行）；§6.3 要求"用重复运行/精度变化的校准 fixture 确定数值噪声范围、校准与验收 fixture 分离"未做；转换带 0.05 与第五轮实测噪声带 mean 0.041 同量级（固定噪声后该噪声已消除，不算失效，但选取逻辑未落字）。本地 git 无独立时间戳，预注册强度止于 commit 序。

**Y3（黄）确定性证据存在但未被引用，且携带性缺陷未修。**
- md 断言"固定噪声下输出确定性成立"，引用的却是三方 sha 两两不同——这只证明路径不同，不证明复跑恒定。真实证据在容器 `w6_harness.log`：**同一日志里有一次崩溃运行（NameError: hashlib_sha，中间版脚本）先打印了三路 sha，随后成功运行写入 JSON 的 sha 与之逐位相同**（ecfd4da2…/b0796424…/347f8c2c…，三路均为跨进程位级复现，含 NPU）。该日志未入库。
- 第五轮 Y1 仍未修：`patches/openpi-jax-resize-hwc.patch` 只有分支一，容器生效代码有 `elif (W,C,H)→HWC` 分支二——验收引用的补丁仍非实际生效代码。
- 小项：`np.save(dict)` 实际落盘 `w6_actions.npz.npy`（pickle，需 allow_pickle）；JSON 未记 noise 数组本身哈希与输入哈希（W7 锚定复现性打折）。

## 绿

- **G1 固定噪声有效性（代码+实测双重验证）**：容器 `policy.py` infer 与 upstream 逐行一致——noise 参数 torch `from_numpy().to(device)` / JAX `jnp.asarray`，ndim==2 自动 `[None]` 加 batch；torch 侧仅 `noise=None` 才走 `sample_noise` 随机回退，显式 noise 直接作 `x_t`（Euler 10 步，dt=-1/10，两框架时间约定一致）。实测（本轮容器内新起进程复跑 torch_cpu）：同 seed sha **逐位复现** `ecfd4da2716dd391`；换 seed(999) 输出 sha 全变（mean 0.037 / max 0.168，与第五轮噪声带同量级）——证明 noise 真实被消费、无静默回退。加上 w6_harness.log 内崩溃运行与成功运行的 sha 逐位一致，三路（含 NPU bf16 路径）跨进程确定性成立。
- **G2 对比无混杂**：torch 侧（同事 assets）与 JAX 侧（原 ckpt assets）norm_stats md5 相同（e1a77bec…）；harness `build_torch_policy` 与 pi05_service.build_policy 同构（同 Pi0Config(pi05=True, pytorch_compile_mode=None)/repack/norm_stats，仅设备参数化）；noise 用模型内部 (50,32)（weights config.json action_dim=32/horizon=50 与动态 probe 一致），S26 教训已吸收。
- **G3 产物一致性**：容器 `/workspace/data/w6-conversion-verification.json`、verdict 与库内提交 md5 一致；`w6_actions.npz.npy` 三路数组 sha 与 JSON 记录逐位相符（数字确出自真实运行）；harness 容器副本 md5 == 810fcd8 提交版。
- **G4 撤回史完整**：原 0.02/0.06 撤回节保留在 w6-conversion-verification.md，重写结论与撤回原因（噪声未固定）逻辑闭环。

## 修复清单（按优先级）

1. （Y1）结论补残留或在 harness 补齐：NaN/Inf 显式检查（`np.isfinite` 断言入 harness）、按维冻结门槛、FK 评估、未解码 50×32 对比、`robodojo_pi05_pt` 逐张量映射记录（或明确声明本验证仅行为级）；据此把 "W6 CLOSED" 改为与 §6.3 对齐的受限表述。
2. （Y2）在 w6-conversion-verification.md 记录 0.01/0.05 的选取依据（与第五轮噪声带、动作分辨率的关系），并声明无校准 fixture；W7 数值门前补一次 K 次重复运行校准。
3. （Y3）把 w6_harness.log（或其关键段）入库并在 md 引用"崩溃运行/成功运行 sha 逐位一致"作为确定性证据，替换现在不成立的 sha 两两不同论据；按服务器实际生效代码重导 resize 补丁（携带项）。
4. （小项）npz 改 `np.savez` 修正扩展名；JSON 补 noise 数组 sha256 与输入观测指纹，保证 W7 锚点可独立复现。

**证据文件**：`scripts/w6_fixed_noise_harness.py`、`docs/acceptance/w6-conversion-verification.{json,md}`、`docs/acceptance/w6-verdict.json`、`docs/acceptance/w6-handover.md`、`patches/openpi-jax-resize-hwc.patch`（库内）；容器 `/workspace/data/w6_harness.log`、`w6_actions.npz.npy`（服务器，本轮抽查输出见上）。
