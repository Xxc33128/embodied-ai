# LP-A1 P1 修复对抗性审查

- 审查人：独立对抗性审查员
- 日期：2026-09-19
- 被审对象：commit `4c4dbcd`（LP-A1 P1 fix）与 `1e8139b`（探针捕获顺序自纠）。`git log --oneline -6`、`git show --stat`、`git status --short` 确认：两 commit 共触及 11+9 个文件，全部在 LP-A1 范围内；工作区唯一未跟踪文件 `scripts/w11_pull_convert_push.sh` 属并行会话，不在范围。
- 审查方式：本地只读代码审阅 + 从 records 离线复算聚合 + 只读 ssh/docker exec（仅 ls/cat/grep/md5sum/sed，未修改服务器与容器任何状态）+ Mac 本地 pytest。
- 原审查解除条件（2026-09-18-LP-A1对抗性审查.md P1-1）：(a) 3 次 reset 各哈希 fixture 根 body 位姿；(b) 同一物理状态连续渲染 2 次做 image_diff；(c) fixture 位姿哈希并入指纹或显式声明协议内在扰动——**三条全部满足**（(c) 两者都做了：`fixture_pose_digest`/`fingerprint_full` + lock `lp_a1.fixture_drift.protocol_declaration`）。

---

## 一、判定

**PASS（无 P1/P2 发现）。**

被审六条声明全部经独立证据复核成立，无一发现夸大到需要降级的程度；唯一实质性瑕疵是交接文档残留三处未同步的旧表述（P3-1/P3-2），其中一处与修正后归因直接矛盾，但不影响验收结论、代码、测试与四份状态文档中修复相关数字的正确性。LP-A1 P1 修复循环可关闭。

核心数字独立复核（全部命中）：

| 声明 | 复算结果 |
|---|---|
| movables 指纹 init0=`7013dce1…`、init1=`9fe5fae0…`（各 3 次 reset 一致） | ✓ 与 v1 evidence（90f80b6 `lp-a1-fingerprint-evidence.json`）逐字节相同 |
| scene 指纹每 init 3 个互异 | ✓（2×3 条全不同） |
| 漂移 stove ~12mm / wine_rack ~15mm / cabinet ~9mm，z=0 | ✓（init1 stove 0.011881 / init0 wine_rack 0.014887 / init0 cabinet 0.008945，z 全 0.0） |
| 渲染器地板 agentview 0.0019、腕部 0.04–0.09，预注册判据双相机通过 | ✓（0.0019；0.0365–0.0932，为"0.04–0.09"的源数据，2 位舍入） |
| 跨 reset agentview 2.88–2.91、腕部 4.99–5.56 = 主导项 | ✓（2.8757/2.908、5.5617/4.9854；主导比 mean 口径 1513–1531×/61.9×） |
| 两跑确定性逐位一致 | ✓ 且强于声明：除 scene 指纹/漂移外，`fingerprint_full`、`fixture_poses`、`bddl_regions`、`cross_reset_render_diff_agg` 全部逐位一致 |

---

## 二、分级发现

### P1（验收结论不可靠）

无。

### P2（显著缺口）

无。

### P3（建议）

- **P3-1 交接文档 §5 残留与修复直接矛盾的旧句**：`2026-09-18-工程交接文档.md` L73（§5"渲染"条）仍写"同状态多次渲染存在逐次微小差（mean≈3/255），**归因未隔离**……**腕部相机尚未纳入监控**（LP-A1 审查 P2）"。与同节 L70（2026-09-19 已更新为"归因已隔离（真同状态双渲染）……腕部 0.04–0.09"）及 §6 第 2 条（P2 已关闭）自相矛盾。1e8139b 只改了 L70 一行，漏改同节 L73。声明 6"四处口径一致"在本句上不成立（其余修复相关数字四处一致）。
- **P3-2 交接文档把 ~1.0 灰度误标为"渲染器地板/渲染噪声"**：同文件 §6 第 2 条"腕部相机纳入 image_diff_stats 监控（**其渲染器地板 ~1.0 灰度**）"、§7 LP-A2 行"腕部**渲染噪声**（~1.0 灰度）"。修正后归因：渲染器地板 0.04–0.09，~1.0 是 3 个穿插 dummy 步的真实视觉变化（lock `lp_a1.render_discovery.wrist_camera` 已正确改写为"对臂/夹爪微小状态变化敏感（3 dummy 步 ~1.0 灰度）"，交接文档未跟）。量级上把 ~1.0 计入 LP-A2 噪声模型仍是对的（保守），但来源标注错误。
- **P3-3 漂移包络测试门比口径松**：`tests/test_libero_session.py` L71-77 的 bound = 全部 region 边长最大值 + 2e-3 = 0.08（`main_table_stove_front_region` 8cm×8cm）+ 0.002 = **0.082**，而三个 fixture 自身 region 均 2cm×2cm（evidence `bddl_regions` 实证）。一个漂到 5cm 的 fixture 仍可通过该测试；"超界说明采样器/协议异常"的检出力只到 8cm 级。建议按 fixture→自身 region 绑定（数据已支持：18/18 个位姿均在各自 region 内，本审查独立验证）。lock/runtime 写"测试门=逐轴 ≤ region 最大边长+2e-3"措辞含糊但与测试代码一致，不算失实。
- **P3-4 指纹对 fixture 内部关节/site 无直接覆盖**：`fingerprint_full` = movables 硬门 + fixture 根 body pos/quat，柜门/抽屉/炉钮关节角与 fixture 附着 site 均不哈希。跨 reset 它们经 `set_init_state` 的 qpos 恢复具有确定性（qpos 含全部关节），本任务（bowl on stove）亦无此类关节，故不构成本次证据漏洞；但接入 libero_10 等带门/抽屉任务族时建议把 fixture 关节角并入 digest。`body_quat` 已入 digest，但测试门只做 body_pos 1mm 扰动，quat 敏感性无回归用例（6 条 records 中 quat 恒定，region yaw 为定点采样，无实际漂移发生）。
- **P3-5 `fixture_pose_digest` 空集退化行为**：`fixtures_dict` 为空的 suite 会得到空输入 sha256 常数 digest（`e3b0c442…`），`fingerprint_full` 退化为 movables 单层。当前任务不触发（已核实本任务 BDDL `:fixtures` 共 4 项：`main_table` 被问题类 L45 `if fixture_category == "table": continue` 显式跳过属设计、桌面位姿不重采样；`bowl_drainer_1` 只是模板残留 region、无 fixture 加载——**无漏 fixture**）。建议加一条非空守卫断言。
- **P3-6 4c4dbcd 的 commit message 曾超前于其证据的预注册 verdict**：该 message 写"renderer floor (agentview 0.05-0.08, wrist 0.94-1.03)…fixture resampling dominates"，而同一 commit 存档的 evidence `verdicts.renderer_bit_stable_same_state=false`、`attribution=renderer_nondeterminism_present`（腕部 1.03>1.0 触发预注册判据失败）。1e8139b 已修正且两版 run log/evidence 均诚实留档（git 历史不可改，自纠链条完整可审计），但"message 先下结论、后改捕获顺序"本身是流程风险样本。
- **P3-7 数字舍入口径（记录性）**：文档"腕部 0.04–0.09"源数据为 0.0365–0.0932；"1500×/60×+"为 mean/mean 口径（max/max 口径腕部为 53.8×；测试哨兵自用保守的 3× 判据）。均在合理舍入范围，不算失实。
- **P3-8（范围外，先前遗留）**：交接文档 §6 第 5 条"Mac 侧 test_kinematics 因缺 mujoco 15 error"——实测 15 error = test_kinematics 7 + test_environment_contract 8，数字对、文件归因错一半。非被审 commit 引入。

---

## 三、确认无误清单（独立复核通过）

1. **证据真实性**：本地 `docs/acceptance/` 三件 md5 与服务器逐字节一致——`lp-a1-fixture-drift-evidence.json` ↔ `/data_nv0/gpt-as-policy-repro/data/lp_a1_fixture_drift.json`（`76d40dd7…`）、`lp-a1-tests-gap-sim.log` ↔ `lp_a1_tests_gap_sim.log`（`ede2e85e…`）、`lp-a1-fixture-drift-run.log` ↔ `lp_a1_fixture_drift_run.log`（`49a4b7df…`）。服务器文件名为下划线风格，内容一致（任务书里的"/data 下同名"按内容对上）。服务器 mtime（00:58/00:58/01:07）与两 commit 时间线（00:54:37、01:07:59 +0800）吻合。
2. **从 records 重算聚合全部命中**：state 指纹 init0 ×3=`7013dce1…`、init1 ×3=`9fe5fae0…`；scene 指纹每 init 3 互异；漂移 6 组逐轴值与 aggregates 逐位相同；same_state 聚合（n=3 的 mean/max）复算相等；renderer_stable verdict 由 12 条 diff 复算为 True 且每条均在预注册阈值内（max mean_abs=0.0932≤1.0，max frac_gt30=0.0032≤0.01）。
3. **两跑确定性（强于声明）**：`git show 4c4dbcd:evidence` vs 当前 evidence——6 条 records 的 `scene_fingerprint`、`fixture_poses`、`state_fingerprint`、`fingerprint_full` 全部逐位一致；`cross_reset_render_diff_agg` 8 组、`bddl_regions` 亦逐位一致；仅 `same_state_render_diff` 不同（捕获点移动的预期效果）。跨 reset diff 数值在两跑间逐位相同尤其有力（说明 obs 捕获点在两版探针中实际相同，混淆只发生在 rerender 一侧）。
4. **预注册可查证**：`RENDERER_STABLE_MEAN_ABS=1.0 / FRAC_GT30=0.01` 在 4c4dbcd 的探针脚本中已存在（`git show 4c4dbcd:scripts/lp_a1_fixture_drift_probe.py`），先于重跑结果；run-1 按该判据判 False 并如实入档。
5. **代码正确性**：`fingerprint_state` 与 90f80b6 版逐字节兼容（同序 4 键：joint_pos→eef_pos→gripper_qpos→object-state，`ascontiguousarray().astype(float64).tobytes()`）；新版把硬编码键改写为循环 `FINGERPRINT_STATE_KEYS` 常量且常量含 `object-state`（修复了 v1 常量/函数体不一致，P3-3 关闭成立）；`fixture_poses` 按 `sorted(fixtures_dict)` 排序、body_pos/body_quat float64 摘要构造规范；`fingerprint_full` = sha256(state 摘要 bytes ‖ scene 摘要 bytes) 与声明一致。
6. **测试链条有效**：1mm 微扰用例（perturb→双断言→finally 恢复→恢复断言）完整；包络测试 ranges 语义 [xmin,ymin,xmax,ymax] 与 BDDL 原文、evidence regions 三方一致；同状态双渲染哨兵断言与探针终版数字匹配（直接判据 + 3× 主导）。region 全空时 `assert extents` 大声失败而非静默通过。
7. **探针捕获顺序（重跑版）**：obs（reset_to 内部 10 wait 步后返回）→ `obs_rerender=_get_observations(force_update=True)` → state_fp → poses → scene_fp → same_state_diff → 3 个穿插 dummy 步 → cross diff（obs_prev vs obs，两端均为各自 reset_to 返回后、穿插步前的观测）。与 v1 协议（`lp_a1_reset_fingerprint.py`）同构；未发现其他混淆（regions 为静态配置、obs_prev 不跨 init 泄漏、dummy 步不消耗 np.random）。
8. **`_get_observations(force_update=True)` 被动性**：容器内实读 robosuite 1.4.0 `environments/base.py`（L323 附近）——仅 `self._update_observables(force=True)`，无 sim.step、无状态写入；渲染不改物理场。配合同状态 diff 实测 0.0019/0.036–0.09，"真同物理状态重渲染"成立。
9. **首版混淆自纠诚实**：4c4dbcd 版探针确在 rerender 前执行 3 个 dummy 步（git 原文证实）；其存档 run log 腕部 0.9197–1.0308、verdict false；1e8139b message/run log/lock/测试注释四处一致地记录了误归因与修正。
10. **Mac 测试套未破坏**：`python3 -m pytest tests/ -q` → 93 passed, 1 skipped（test_libero_session importorskip，预期）, 6 xfailed, 1 xpassed, 15 errors（mujoco 缺失的已知环境限制，test_kinematics 7 + test_environment_contract 8，范围外）。
11. **四份状态文档的修复相关数字一致**：lock `panel_status`/`lp_a1` 节（0.0019、0.04–0.09、2.88–2.91、4.99–5.56、漂移表 [0.008,0.0119]/[0.0149,0.0098]/[0.0089,0.0055]（=两 init 逐轴最大值的 4 位舍入，可对账）、md5 76d40dd7/ede2e85e/49a4b7df、v1 交叉锚）全部与 evidence 一致；runtime.md LP-A1 节一致；README 当前状态节一致；交接文档 §1/§5 指纹协议条/§6.1/§6.4/§7 一致（P3-1/P3-2 的三句残留除外）。lock 容差推导可溯源：3.4074（init0 agentview cross max）×1.5→5.0、0.0698（init1 frac_gt30 max）×1.4→0.10。
12. **commit 卫生**：4c4dbcd message 中"Evidence md5 both sides: 300385b4/d33c796c"经 `git show … | md5` 核实；两 commit 只触及范围内文件；工作区除范围外未跟踪 w11 脚本外干净。
13. **包络数据复核**：18 个 fixture 位姿逐一验证在各自 BDDL region 内（stove/wine_rack/cabinet 均 2cm×2cm）；z 全部恒定（0.905/0.92）。

---

## 四、必查清单逐问回答

**Q1 证据真实性**：三件 md5 双侧一致（见确认清单 1）。服务器侧实际文件名为 `lp_a1_fixture_drift.json`/`lp_a1_tests_gap_sim.log`/`lp_a1_fixture_drift_run.log`（下划线），与本地连字符文件名不同但内容逐字节一致。

**Q2 从 records 重算聚合**：全部可复算且相等（见确认清单 2）。预注册阈值真实写在探针脚本常量中，且 `git show 4c4dbcd` 证明其在出重跑结果之前已提交可查；run-1 按判据如实判 False。

**Q3 代码正确性**：`fingerprint_state` 与 90f80b6 逐字节兼容（实证：新旧证据同 digest）；fixture_poses 排序/float64 摘要构造正确；fingerprint_full 组合方式与声明一致；`FINGERPRINT_STATE_KEYS` 与函数体经循环构造天然一致且契约测试双向锁定。

**Q4 测试有效性**：1mm 用例链条完整有效（扰动的是 digest 实际读取的 `model.body_pos`，无需 step 即生效）；包络测试 ranges 语义正确、能困住异常但门值偏松（P3-3）；哨兵与探针数据匹配。

**Q5 探针捕获顺序**：重跑版全部捕获（obs/obs_rerender/poses/state_fp/scene_fp/same_state_diff）在 3 个穿插 dummy 步之前；cross diff 数据端点与 v1 同构（相邻 rep 的 reset_to 返回后、穿插步前）；无其他混淆。两跑 cross diff 逐位一致进一步佐证捕获点一致。

**Q6 `_get_observations` 被动性**：容器内 grep 实读确认无物理状态写入，真同状态重渲染成立。

**Q7 确定性声明**：成立且更强——除声明的 scene 指纹/漂移外，poses、state_fp、fingerprint_full、bddl_regions、cross_reset 聚合均逐位一致；首版 records 中仅 `same_state_render_diff` 不同，与"捕获点变了"的声明精确吻合。

**Q8 四处一致**：修复相关数字与结论四处一致、与 evidence 一致、lock 内 md5 引用正确（见确认清单 11）；例外为交接文档三句残留（P3-1/P3-2）。

**Q9 反例攻击**：无漏 fixture（table 被问题类显式跳过且不重采样、bowl_drainer_1 无 fixture 加载，容器内源码/BDDL 实证）；盲点为 fixture 内部关节/site（P3-4，当前任务不触发）、空 fixtures_dict 退化（P3-5）、quat 无回归用例（P3-4）、包络门松（P3-3）。

**Q10 流程卫生**：commit message 与内容一致（4c4dbcd 的 message 曾超前于 verdict，P3-6，已由 1e8139b 诚实纠正）；只提交范围内文件；Mac 测试套无回归。

---

## 五、证据命令一览

```
git log --oneline -6 && git show --stat 4c4dbcd 1e8139b && git status --short
md5 -q docs/acceptance/lp-a1-fixture-drift-evidence.json docs/acceptance/lp-a1-tests-gap-sim.log docs/acceptance/lp-a1-fixture-drift-run.log
# -> 76d40dd7… / ede2e85e… / 49a4b7df…
ssh <实验服务器> 'md5sum /data_nv0/gpt-as-policy-repro/data/lp_a1_fixture_drift.json lp_a1_tests_gap_sim.log lp_a1_fixture_drift_run.log'   # 三者一致
python3 - <<EOF  # records 重算：state/scene 指纹、漂移、same/cross 聚合、verdicts、主导比、两跑逐位对比、18 位姿 region 包络、quat 恒定性
git show 90f80b6:docs/acceptance/lp-a1-fingerprint-evidence.json   # v1 指纹 7013dce1/9fe5fae0 逐字节锚
git show 4c4dbcd:scripts/lp_a1_fixture_drift_probe.py             # 预注册阈值在先 + 首版捕获顺序混淆实证
git show 4c4dbcd:docs/acceptance/lp-a1-fixture-drift-evidence.json | md5   # 300385b4 与 message 一致
ssh <实验服务器> 'docker exec gap-sim sed -n "300,345p" …/robosuite/environments/base.py'   # _get_observations 被动
ssh <实验服务器> 'docker exec gap-sim cat …/bddl_files/libero_goal/put_the_bowl_on_the_stove.bddl'   # :fixtures 4 项
ssh <实验服务器> 'docker exec gap-sim sed -n "40,70p" …/problems/libero_tabletop_manipulation.py'    # table 显式跳过
python3 -m pytest tests/ -q   # 93 passed, 1 skipped, 6 xfailed, 1 xpassed, 15 errors(mujoco 缺失)
```
